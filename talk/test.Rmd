```{r setup, include=FALSE}
opts_chunk$set(tidy = FALSE) # preserve wrapped code
opts_chunk$set(dev = 'pdf') # size plots better
opts_chunk$set(fig.cap = "")
require(reshape2)
require(ggplot2)
require(grid) # for "unit" function for ggplot legend size
```

### Slide 1
    <span style='font-size:9.0pt;font-family:"Arial">
    Kay Gregory is a Journalist and Professional
    Speaker who is kept busy as an M.C, Marriage
    and Funeral Celebrant, Pubic Relations consultant,
    Television News Producer, and Newsreader for
    Radio Live.
    <o:p></o:p></span>